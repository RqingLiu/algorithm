# 示例：本项目从环境变量读取配置，不自动加载 .env。
# 启动前可运行 set -a; source .env; set +a
MODEL_MODE=demo
OLLAMA_URL=http://localhost:11434
EMBED_MODEL=
CHAT_MODEL=
# 不设置 POLICIES_PATH 时使用项目根目录的 policies.json。
# POLICIES_PATH=./local-data/policies.json
